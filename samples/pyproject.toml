[project]
name = "samples"
version = "0.1.0"
description = "Chat application with Ollama llama3.1"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "dotenv>=0.9.9",
    "langchain",
    "langchain-community",
    "requests>=2.31.0",
]
